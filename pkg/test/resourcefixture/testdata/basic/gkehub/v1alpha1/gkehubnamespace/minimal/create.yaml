apiVersion: gkehub.cnrm.cloud.google.com/v1alpha1
kind: GKEHubNamespace
metadata:
  name: gkh-ns-min-${uniqueId}
spec:
  projectRef:
    external: projects/${projectId}
  scopeRef:
    name: gkehubnamespace-dep-min-${uniqueId}
